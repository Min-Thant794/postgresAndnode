export const cloudinaryFolders = {
    userProfile: (userId: string) => `users/${userId}/profile`,
    userDocuments: (userId: string) => `users/${userId}/documents`,

    // productImages: (productId: string) => `products/${productId}/images`,

    // postImage: (postId: string) => `posts/${postId}/images`,

    // tempUploads: () => "temp/uploads",
};